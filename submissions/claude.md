# Claude Code distribution

Install from the Tempo marketplace:

```bash
claude plugin marketplace add tempoxyz/plugins
claude plugin install docs@tempo
```

For the official marketplace, submit from
`https://platform.claude.com/plugins/submit`. Submit each plugin independently;
start with `docs`.
