{
  "name": "@tempoxyz/plugins",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:gemini": "tsx scripts/build-gemini.ts",
    "check:types": "tsc --noEmit",
    "test": "npm run check:types && npm run build:gemini && vitest run",
    "test:mcp": "vitest run --config vitest.mcp.config.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
